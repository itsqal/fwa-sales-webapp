# AI Agents
claude/
CLAUDE.md

# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
# ...except the template, which documents what has to be set.
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts


# docs
# Internal planning and review documents stay out of the repository —
# CLAUDE.md, the backend spec, the development plan, the UI review. Only the
# README, which explains how to run this thing, is committed.
*.md
!README.md
