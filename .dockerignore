# Keep the build context small and free of secrets.

node_modules/
.next/
out/

# Never let a real .env into an image layer. `.env.example` is a template and
# carries nothing secret.
.env
.env.*
!.env.example

.git/
.claude/
.vscode/
.idea/

# Source material for the build, not input to it: 23 mockup PNGs, the OpenAPI
# contract, the asset pack and its generators. `references/openapi.yaml` is only
# needed by `npm run api:types`, which runs on a developer's machine — the
# generated `src/lib/api/schema.d.ts` is what the build compiles.
Full UI per use case/
references/
hifiair-web-assets/
hifiair-app-assets/

*.md
!README.md

npm-debug.log*
*.tsbuildinfo
